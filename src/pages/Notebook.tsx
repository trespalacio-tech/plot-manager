import { PageContainer } from '@/components/PageContainer';

export function NotebookPage(): JSX.Element {
  return (
    <PageContainer
      title="Cuaderno de campo"
      subtitle="Registro diario de operaciones. Disponible desde el Sprint 4."
    />
  );
}
