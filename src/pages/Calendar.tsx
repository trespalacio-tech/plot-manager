import { PageContainer } from '@/components/PageContainer';

export function CalendarPage(): JSX.Element {
  return (
    <PageContainer
      title="Calendario"
      subtitle="Vista mensual y semanal de tareas. Disponible desde el Sprint 5."
    />
  );
}
