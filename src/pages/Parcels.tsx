import { PageContainer } from '@/components/PageContainer';

export function ParcelsPage(): JSX.Element {
  return (
    <PageContainer
      title="Parcelas"
      subtitle="CRUD de fincas y parcelas con mapa. Disponible desde el Sprint 2."
    />
  );
}
