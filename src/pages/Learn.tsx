import { PageContainer } from '@/components/PageContainer';

export function LearnPage(): JSX.Element {
  return (
    <PageContainer
      title="Aprender"
      subtitle="Fichas, glosario y bibliografía. Disponible desde el Sprint 6."
    />
  );
}
